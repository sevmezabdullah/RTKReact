/* eslint-disable prettier/prettier */
export type Register = {
    email: string;
    password: string;
    name: string;
    surname: string;
  };
  export type Login = {
    email: string;
    password: string;
    lang: string;
  };
  export type User = {
    email:string,
    name:string,
    surname:string
  };